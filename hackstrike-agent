#!/bin/bash
# Quick launcher for HackStrike AI Agent

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/mcp-agent"

# Check if built
if [ ! -f "dist/cli.js" ]; then
    echo "Building project..."
    npm run build
fi

# Run the agent
exec node dist/cli.js "$@"
